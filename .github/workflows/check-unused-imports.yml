name: Check for unused imports using Pylint
on:
  pull_request:
    branches:
      - circinus
    paths:
      - '**'
      - '!.github/**'
      - '!**/*.md'
  workflow_dispatch:

permissions:
  pull-requests: write
  contents: read

jobs:
  check-unused-imports:
    uses: vyos-networks/.github/.github/workflows/check-unused-imports.yml@circinus
    secrets: inherit
